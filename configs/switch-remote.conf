! Remote Layer-2 switch
enable
configure terminal

hostname SW-REMOTE-LAN

vlan 20
 name REMOTE-LAN
exit

interface range g0/0 - 2
 switchport mode access
 switchport access vlan 20
 no shutdown
exit

end
write
